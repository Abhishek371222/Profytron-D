# Accessibility Report — Phase 6

- Reduced motion: GPU Minimal / hero skips WebGL density; coach orb animations disabled via CSS media query.
- Hero decorative: `aria-hidden` on ambient visual.
- CoachOrb: `role="img"` + aria-label per emotion.
- Keyboard: existing chatbot/input flows unchanged.
- No dashboard a11y regressions introduced.
