# B3 — Parameter Intelligence

Instead of bare `ATR = 14`, Coach/UI explains: what the parameter does, sensitivity, risk tradeoffs.

Surface next to fields in Builder; deep “why” can open Coach with strategy context.
