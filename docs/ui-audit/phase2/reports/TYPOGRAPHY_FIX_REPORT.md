# Typography Fix Report

**Debt cite:** `systemic__typography-truncate` + TYPOGRAPHY_AUDIT wrap notes

## Changes (no type-scale redesign)

| Change | Where |
| --- | --- |
| `.shell-title` utility | `min-w-0` + ellipsis truncation |
| Applied | TopBar display name, connected-accounts bot name, subscriptions bot name |
| Description cells | `truncate` + max-width on billing description (md+) |

## Not changed

Heading/body font sizes and weights — inventory only; no redesign.
