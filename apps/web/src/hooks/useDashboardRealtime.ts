/**
 * Legacy facade — realtime bridge lives in platform/dashboard.
 */
export { useDashboardRealtime } from '@/platform/dashboard/useDashboardRealtime';
export { reconnectTradingSocket } from '@/lib/realtime/trading-socket';
